# REST polling support

This package provides bounded requests, pagination, compact JSON, and acknowledged-delivery checkpoints for the containing ingester. Run its tests from the registered source checkout with `go test -race ./hosted/plugins/slack/...`.
